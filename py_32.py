# !/usr/bin/python
# -*- coding: UTF-8 -*-


# 切片，即list取值的一种方式，取值是顾头不顾尾
# 切片的步长默认为1，不可为0
# [首:尾:步长]一般有三个值，
# 第一个为首个元素起点，第二个为结尾元素终点，但是不包括这个元素，
# 顾头不顾尾，第三个值为步长，默认为1，不可以为0。
# [1:5:2] 要取索引为1到4的值，步长为2
# [::2] 要取所有的值，步长为2，如：[1,2,3,4,5],
# 取到的值为[1,3,5]
# [::-1] 切片步长为负数的话，是从后面往前取值，相当于翻转了
# 步长为负数，前面为正数的，取出来就是空的了


a = ['one', 'two', 'three']
for i in a[::-1]:
    print i
